pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'hostname'
            }
        }
        stage("Testname"){
            steps {
                echo "Test stage"
            }
        }
    }
}
